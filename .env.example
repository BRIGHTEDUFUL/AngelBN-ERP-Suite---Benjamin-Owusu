# Environment variables for local development
# Copy to .env and set values

# Google Gemini API key used by services/geminiService.ts
GEMINI_API_KEY=your_api_key_here

# Base path for GitHub Pages project site deployment
# For username.github.io/<repo>, set to "/<repo>/"
# For username.github.io (user/organization page), set to "/"
BASE_PATH=/abn-broadcast-dashboard
